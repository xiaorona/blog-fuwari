---
title: 简单的起始页
published: 2025-05-02
description: "简单的起始页，显示时间，可切换搜索引擎。"
image: ""
tags: ["网页"]
category: 源码
draft: false
---

预览：[起始页](star.sylu.net)
下面是一个简单的浏览器起始页，可自定义LOGO`images/logo.png`，搜索框上方显示时间，可切换百度、必应和谷歌搜索，深浅色切换。
预览页
```
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>起始页</title>
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { 
            margin: 0; 
            padding: 5vh 0 0; 
            display: flex; 
            flex-direction: column; 
            align-items: center; 
            height: 100vh; 
            background: #80CAFF; 
            transition: background-color 0.3s ease, color 0.3s ease; 
        }
        body.dark-mode { 
            background: #000000; 
            color: #ffffff; 
        }
        .logo { width: 100px; margin-bottom: 20px; }
        .time-display { font: bold 48px 'Lato', sans-serif; color: black; margin-bottom: 10px; }
        .dark-mode .time-display { color: white; }
        .search-container { width: 61.8%; max-width: 600px; text-align: center; margin-bottom: 160px; position: relative; }
        .search-form { display: flex; align-items: center; justify-content: center; background: rgba(255, 255, 255, 0.2); backdrop-filter: blur(10px); border-radius: 25px; padding: 5px; box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); transition: background-color 0.3s ease; }
        .search-form.focused { background-color: rgba(255, 255, 255, 1); }
        .search-box { 
            width: 100%; 
            padding: 15px 20px; 
            border: none; 
            border-radius: 25px; 
            font-size: 16px; 
            outline: none; 
            background: transparent; 
            color: #000; 
            transition: padding 0.3s ease; 
            text-align: center; 
        }
        .search-box:focus { color: #000 !important; }
        .search-box.has-text { padding-left: 80px; }
        .search-box::placeholder { color: rgba(0, 0, 0, 0.7); }
        .dark-mode .search-box { color: #fff; }
        .dark-mode .search-box::placeholder { color: rgba(255, 255, 255, 0.7); }
        .engine-button { position: absolute; padding: 12px 20px; border: none; border-radius: 25px; background-color: #80CAFF; color: white; font-size: 16px; cursor: pointer; transition: background-color 0.3s ease; margin: 5px; left: 5px; }
        .engine-button:hover { background-color: #005bb5; }
        .engine-dropdown { 
            position: absolute; 
            left: 0; 
            top: 60px; 
            width: auto; 
            min-width: 80px; 
            background: rgba(255, 255, 255, 0.2); 
            backdrop-filter: blur(20px); 
            border-radius: 20px; 
            padding: 10px 10px; 
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); 
            display: none; 
            z-index: 1000; 
            text-align: center; 
        }
        .engine-dropdown button { 
            display: block; 
            width: 100%; 
            padding: 8px 10px; 
            border: none; 
            background: none; 
            color: #000; 
            text-align: center; 
            cursor: pointer; 
        }
        .dark-mode .engine-dropdown button { color: #fff; }
        .engine-dropdown button:hover { background-color: rgba(255, 255, 255, 0.1); }
        .mode-toggle-button {
            position: fixed;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            padding: 10px 20px;
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            border: none;
            border-radius: 20px;
            color: #000;
            font-size: 14px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .dark-mode .mode-toggle-button { color: #fff; }
        .mode-toggle-button:hover { background-color: rgba(255, 255, 255, 0.4); }
        @media (max-width: 768px) {
            .logo { width: 80px; }
            .time-display { font-size: 36px; }
            .search-container { width: 90%; }
            .search-box { padding: 12px 15px; font-size: 14px; }
            .engine-button { padding: 10px 15px; font-size: 14px; }
        }
    </style>
</head>
<body>
    <img class="logo" src="images/logo.png" alt="Logo">
    <div class="time-display" id="timeDisplay"></div>
    <div class="search-container">
        <form class="search-form" id="searchForm" action="https://www.baidu.com/s" method="GET">
            <button type="button" class="engine-button" id="engineButton">百度</button>
            <div class="engine-dropdown" id="engineDropdown">
                <button type="button" onclick="changeEngine('百度', 'https://www.baidu.com/s', 'wd')">百度</button>
                <button type="button" onclick="changeEngine('必应', 'https://www.bing.com/search', 'q')">必应</button>
                <button type="button" onclick="changeEngine('谷歌', 'https://www.google.com/search', 'q')">谷歌</button>
            </div>
            <input type="text" name="wd" class="search-box" placeholder="搜索">
        </form>
    </div>
    <button class="mode-toggle-button" onclick="toggleDarkMode()">深色</button>

    <script>
        function toggleDarkMode() {
            const body = document.body;
            body.classList.toggle('dark-mode');
            const modeToggleButton = document.querySelector('.mode-toggle-button');
            modeToggleButton.textContent = body.classList.contains('dark-mode') ? '浅色' : '深色';
        }

        function updateTime() {
            const now = new Date();
            const hours = String(now.getHours()).padStart(2, '0');
            const minutes = String(now.getMinutes()).padStart(2, '0');
            document.getElementById('timeDisplay').textContent = `${hours}:${minutes}`;
        }
        updateTime();
        setInterval(updateTime, 1000);

        const searchBox = document.querySelector('.search-box');
        const searchForm = document.querySelector('.search-form');
        const engineButton = document.getElementById('engineButton');
        const engineDropdown = document.getElementById('engineDropdown');

        searchBox.addEventListener('focus', () => {
            searchForm.classList.add('focused');
            searchBox.classList.add('focused');
            engineButton.style.display = 'none';
        });

        searchBox.addEventListener('blur', () => {
            searchForm.classList.remove('focused');
            searchBox.classList.remove('focused');
            engineButton.style.display = searchBox.value.trim() === '' ? 'block' : 'block';
        });

        engineButton.addEventListener('click', (event) => {
            event.stopPropagation();
            engineDropdown.style.display = engineDropdown.style.display === 'block' ? 'none' : 'block';
        });

        document.addEventListener('click', (event) => {
            if (!engineDropdown.contains(event.target) && event.target !== engineButton) {
                engineDropdown.style.display = 'none';
            }
        });

        function changeEngine(name, url, paramName) {
            engineButton.textContent = name;
            searchForm.action = url;
            searchBox.name = paramName;
            engineDropdown.style.display = 'none';
            localStorage.setItem('selectedEngine', JSON.stringify({ name, url, paramName }));
        }

        function loadSelectedEngine() {
            const selectedEngine = JSON.parse(localStorage.getItem('selectedEngine'));
            if (selectedEngine) changeEngine(selectedEngine.name, selectedEngine.url, selectedEngine.paramName);
        }
        loadSelectedEngine();

        searchForm.addEventListener('submit', (event) => {
            if (searchBox.value.trim() === '') {
                event.preventDefault();
                alert('请输入搜索内容');
            }
        });
    </script>
</body>
</html>
```